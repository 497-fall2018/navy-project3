import { Dimensions } from 'react-native';

export default {
    cardImageStyle: {
        width: Dimensions.get('window').width * 0.93,
        height: 200,
        borderRadius: 20,
        alignSelf: 'center',
    },
}
